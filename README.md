# Dental-MAE-SSL
Self-supervised learning project using Masked Autoencoders (MAE) on dental radiographs. Trained a Vision Transformer to reconstruct masked image regions, enabling meaningful feature extraction without labels. Focused on pretraining, not downstream tasks.
